import ResetPasswordComponents from '../components/Reset Password/ResetPasswordComponents';

const ResetPasswordView = () => {
  return (
    <>
      <ResetPasswordComponents />
    </>
  );
};

export default ResetPasswordView;
